# Copy to .env.local for local development. Next.js loads .env.local
# automatically; it is gitignored by the default Next.js .gitignore rules.

# Must point at the backend's API prefix (see backend/app/main.py router
# prefixes, all mounted under /api/v1).
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
